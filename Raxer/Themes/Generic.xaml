<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:myControls="clr-namespace:Raxer.Controls">

    <!--
        WPF loads this file automatically for custom controls via DefaultStyleKey.
        This is a platform convention, not a project choice.
        See Themes/README.md for details.
    -->

    <!-- ════════════════════════════════════════ -->
    <!-- MY ICON                                  -->
    <!-- ════════════════════════════════════════ -->
    <Style TargetType="{x:Type myControls:Icon}">
        <Setter Property="Width" Value="24" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type myControls:Icon}">
                    <Viewbox>
                        <Path Data="{TemplateBinding Data}"
                              Fill="{StaticResource text}"
                              Stretch="Uniform"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
                    </Viewbox>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════════════════════════════════════════ -->
    <!-- NUMERICUPDOWN                                 -->
    <!-- ════════════════════════════════════════ -->
    <Style TargetType="{x:Type myControls:NumericUpDown}">

        <Setter Property="Width" Value="90"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Background" Value="{StaticResource bg.surface}"/>
        <Setter Property="Foreground" Value="{StaticResource text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>

        <Setter Property="Template">
            <Setter.Value>

                <ControlTemplate
                    TargetType="{x:Type myControls:NumericUpDown}">

                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3"
                        SnapsToDevicePixels="True">

                        <Grid ColumnDefinitions="*,22">

                            <!-- VALUE -->
                            <TextBox
                                x:Name="TextElement"
                                Grid.Column="0"
                                Background="Transparent"
                                BorderThickness="0"
                                Foreground="{TemplateBinding Foreground}"
                                CaretBrush="{StaticResource text}"
                                Padding="6,0"
                                FontSize="{TemplateBinding FontSize}"
                                HorizontalContentAlignment="Right"
                                VerticalContentAlignment="Center"/>

                            <!-- REPEAT BUTTONS -->
                            <Grid
                                Grid.Column="1"
                                RowDefinitions="*,*"
                                Margin="0,2,2,2"
                                >
                                <RepeatButton
                                    x:Name="UpButton"
                                    Grid.Row="0"
                                    Style="{StaticResource stepper_flecha}">
                                    <Path
                                        Width="7"
                                        Height="4"
                                        Fill="{TemplateBinding Foreground}"
                                        Data="M 0,4 L 3.5,0 L 7,4 Z"/>
                                </RepeatButton>
                                <RepeatButton
                                    x:Name="DownButton"
                                    Grid.Row="1"
                                    Style="{StaticResource stepper_flecha}">
                                    <Path
                                        Width="7"
                                        Height="4"
                                        Fill="{TemplateBinding Foreground}"
                                        Data="M 0,0 L 3.5,4 L 7,0 Z"/>
                                </RepeatButton>
                            </Grid>

                        </Grid>

                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource border.focus}"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource border.over}"/>
                        </Trigger>

                        <Trigger Property="IsEnabled"  Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.5"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>
