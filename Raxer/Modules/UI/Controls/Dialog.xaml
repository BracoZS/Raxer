<Window x:Class="Raxer.Modules.UI.Controls.Dialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Raxer.Modules.UI.Controls"
        mc:Ignorable="d"
        Title="Dialog" 
        
        Height="200" Width="300"
        Background="#2D2D2D"
        Foreground="White"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        >
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title bar (draggable) -->
        <Border Grid.Row="0" Background="#252525" Padding="16,12"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <TextBlock x:Name="TitleText" FontSize="14" FontWeight="SemiBold" Foreground="White"  Text="Placeholder title"/>
        </Border>

        <!-- Message -->
        <TextBlock x:Name="MessageText" Grid.Row="1"
                   FontSize="13" Foreground="#CCCCCC"
                   TextWrapping="Wrap"
                   Margin="16,16,16,0"
                   VerticalAlignment="Top"
                   
                   Text="Place holder text description"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,12,12">
            <Button x:Name="OkButton" Content="OK" Width="80" Height="32" Margin="0,0,8,0"
                    Click="OkButton_Click"
                    Background="#4D9DE0" Foreground="White" BorderThickness="0"
                    Cursor="Hand" />
            <Button x:Name="CancelButton" Content="Cancelar" Width="80" Height="32"
                    Click="CancelButton_Click"
                    Background="#3A3A3A" Foreground="#CCCCCC" BorderThickness="0"
                    Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>
